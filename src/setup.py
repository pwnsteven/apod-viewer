#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='outside',
    version='1.0',
    description='Technical Interview Project',
    author='Outside Engineering',
    author_email='outside@example.com',
    packages=find_packages(),
    include_package_data=True,
)
